.header
  .header__text
    =link_to root_path, class: 'home__link' do
      =icon 'fas', 'snowboarding'
      %p skate・session
  .header__right-nav
    - if user_signed_in?
      -# =@user.nickname
      =link_to "logout", destroy_user_session_path, method: :delete, class: 'header__user-btn'
    - else
      =link_to "login", new_user_session_path, class: 'header__user-btn'
      =link_to "signup", new_user_registration_path, class: 'header__user-btn'
    =link_to '#', class: 'search__link' do
      =icon 'fas', 'search', class: "icon-search"

.contents
  .top__content-text
    %p NEW TOPICS
  .top__contents
    .content__videos
      - @news.each do |topic|
        .content__video
          =link_to topic_path(topic), class: 'topic__link-index' do
            .topics__post{:style => "background-image: url(#{topic.post});"}
%br/
%br/
%br/
%br/

.topics__all-text
  %p ALL TOPICS    

.topics__content-all-index
  - @randams.each do |topic|
    -# .user__topics-show
      -# =image_tag topic.post.url
    .topics__all-index
      =link_to topic_path(topic), class: 'topic__link-show' do
        .topic-post-index{:style => "background-image: url(#{topic.post});"}
%br/
%br/
%br/
%br/
%br/
%br/



-# .content__follow-text
-#   %p フォロー
-#   .content__follow-videos
-#     .follow-video
-#     .follow-video



.footer__nav
  = render 'shared/footer_nav'