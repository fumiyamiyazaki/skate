-# -if @topics.post?
-#   -if @topics.post.file.content_type.include?('image/')
-#     =link_to topic_path(@topic) do
-#       =image_tag @topic.post.url
-#   -else
-#     =link_to topic_path(@topic) do
-#       =video_tag @topic.post.url, controls: true, autobuffer: true
